name: BusyLight
options:
  bundleIdPrefix: com.mboszko
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true

configs:
  Debug: debug
  Release: release

settings:
  base:
    SWIFT_VERSION: "6.0"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    ENABLE_HARDENED_RUNTIME: YES

targets:
  BusyLight:
    type: application
    platform: macOS
    sources:
      - path: BusyLight
        excludes:
          - "**/.DS_Store"
          - "BusyLight.help"
      - path: BusyLight/BusyLight.help
        type: folder
        buildPhase: resources
      - path: AppIcon.icon
        buildPhase: sources
    preBuildScripts:
      - name: "Set Build Number"
        script: |
          PLIST="${PROJECT_DIR}/BusyLight/Info.plist"
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $(date +%y%m%d.%H%M)" "$PLIST"
          GIT_SHA=$(git -C "${PROJECT_DIR}" rev-parse --short HEAD 2>/dev/null || echo "unknown")
          /usr/libexec/PlistBuddy -c "Set :GitShortSHA ${GIT_SHA}" "$PLIST" 2>/dev/null || /usr/libexec/PlistBuddy -c "Add :GitShortSHA string ${GIT_SHA}" "$PLIST"
        basedOnDependencyAnalysis: false
    postBuildScripts:
      - name: "Generate Help Index"
        script: |
          HELP_PLIST="$BUILT_PRODUCTS_DIR/$UNLOCALIZED_RESOURCES_FOLDER_PATH/BusyLight.help/Contents/Info.plist"
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $(date +%y%m%d.%H%M)" "$HELP_PLIST"
          hiutil -I corespotlight -Caf "$BUILT_PRODUCTS_DIR/$UNLOCALIZED_RESOURCES_FOLDER_PATH/BusyLight.help/Contents/Resources/en.lproj/search.helpindex" "$BUILT_PRODUCTS_DIR/$UNLOCALIZED_RESOURCES_FOLDER_PATH/BusyLight.help/Contents/Resources/en.lproj/"
        basedOnDependencyAnalysis: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mboszko.BusyLight
        PRODUCT_NAME: Busy Light
        PRODUCT_MODULE_NAME: BusyLight
        INFOPLIST_FILE: BusyLight/Info.plist
        CODE_SIGN_ENTITLEMENTS: BusyLight/BusyLight.entitlements
        GENERATE_INFOPLIST_FILE: false
        SWIFT_EMIT_LOC_STRINGS: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        COMBINE_HIDPI_IMAGES: YES
        SWIFT_STRICT_CONCURRENCY: complete
      configs:
        Release:
          CODE_SIGN_IDENTITY: "Developer ID Application"
          CODE_SIGN_STYLE: Manual
    entitlements:
      path: BusyLight/BusyLight.entitlements
      properties: {}

  BusyLightTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: BusyLightTests
        excludes:
          - "**/.DS_Store"
    dependencies:
      - target: BusyLight
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.mboszko.BusyLightTests
        GENERATE_INFOPLIST_FILE: YES
        BUNDLE_LOADER: "$(TEST_HOST)"
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Busy Light.app/Contents/MacOS/Busy Light"
        INFOPLIST_KEY_NSPrincipalClass: BusyLightTests.TestSetup

schemes:
  BusyLight:
    build:
      targets:
        BusyLight: all
        BusyLightTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - BusyLightTests
